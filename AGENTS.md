# AGENTS.md — Nuvo

このリポジトリで作業するコーディングエージェント向けの指示書。
**第2章「絶対制約」に反する実装は、どんな理由があっても提案・実装しないこと。**

---

## 1. プロジェクト概要

**アプリ名: Nuvo**

BeautyPlus / SNOW / Ulike 系の美顔・写真加工アプリ。

**コンセプト**

- 全機能を完全オフライン(端末内処理)で提供する
- 開発者側は一切のユーザーデータを保持しない
- **全機能完全無料**(サブスク・買い切り・広告・透かし、すべてなし)

**市場での立ち位置**

既存の主要アプリは、肌補正やフィルターといった基本機能までサブスクの内側に置くか、無料版に透かしを入れる。しかしこれらの処理は端末のGPUで完結し、運用コストはほぼゼロ。
本アプリは「基本機能に課金する理由がない」という立場を取り、全機能を無料開放することを差別化の中心に据える。この方針はUIコピー・ストア説明文・プライバシーポリシーで一貫させる。

**収益モデル**

なし。年間の維持費はApple Developer Program(¥11,800/年)のみ。

**本質的な目的**

短期的にはユーザーを増やすことが目標だが、より長期的な狙いは「開発者側が実質何もしていないのに課金され続けている」現状の業界構造に対する布石を打つこと。
オンデバイスで完結し原価がほぼゼロの機能に対して、サブスクという継続課金を強いるビジネスモデルが当たり前になっている状態そのものに対するカウンターとして本プロジェクトを位置づける。機能や実装の判断に迷ったときは、この立場を損なわない方を選ぶこと。

**オープンソース化の方針**

ソースコードは最終的に公開する。この前提により、以下を通常以上に重視する。

- 恥ずかしいコードを書かない。コメント・命名・構造は「他人に読まれる」前提で書く
- 公開時にAPIキーや個人情報が混入しないよう、機密情報はコードに直接書かない(Secretsは`.gitignore`対象のファイルに分離する)
- ライセンスは公開時に選定する(MIT想定)。依存を増やさない方針とも整合するため、サードパーティのライセンス整理コストは最小限で済む見込み
- 「非公開だから雑でもいい」という判断はしない。設計段階から公開後を意識する

---

## 2. 絶対制約(違反禁止)

エージェントは以下を**提案も実装もしてはならない**。依頼された場合も、実装前に必ず確認を取ること。

| 禁止事項 | 理由 |
|---|---|
| `URLSession` 等によるネットワーク通信 | 完全オフラインが製品価値の中核 |
| 広告SDK(AdMob, AppLovin 等) | トラッキングIDの収集が発生する |
| 解析SDK(Firebase, Amplitude 等) | 同上。クラッシュ解析はXcode Organizer標準機能で代替 |
| `StoreKit` / アプリ内課金 | 完全無料方針 |
| 出力画像への透かし・ロゴ挿入 | 無料の質を落とす典型手法。採用しない |
| 機能のロック・回数制限・「Pro」表記 | 同上 |
| アカウント登録・ログイン | 登録不要が方針 |
| クラウドAI処理(画像の外部送信) | いかなる形でも不可 |
| ATT(App Tracking Transparency) | トラッキングしないため不要 |
| サードパーティ依存の追加 | 原則ゼロ依存。必要な場合は必ず事前確認 |

**判断基準**: 「その実装で開発者や外部サーバーが何らかの情報を得るか?」「ユーザーの手を止めて課金を促すか?」がYESなら実装しない。

---

## 3. 技術スタック

- **言語**: Swift 5.9+
- **UI**: SwiftUI(描画層のみUIKit/Metalを併用)
- **最低サポート**: iOS 17.0
- **顔解析**: `Vision` — `VNDetectFaceLandmarksRequest`(76点ランドマーク)
- **画像処理**: `Core Image` + **カスタム `CIKernel`(Metal)**
- **リアルタイム描画**: `MetalKit` (`MTKView`)
- **カメラ**: `AVFoundation` (`AVCaptureVideoDataOutput`)
- **写真アクセス**: `PhotosUI` (`PhotosPicker`) / `Photos`(保存のみ)
- **外部依存**: なし

---

## 4. 機能スコープ

### フェーズ1: 写真加工(最優先。これ単体でリリース可能)

| カテゴリ | 機能 |
|---|---|
| 肌 | スムージング、美白、血色調整、ニキビ・シミ除去(スポット修復) |
| 顔立ち | 小顔、輪郭、目の拡大、鼻筋、あご |
| メイク | リップ、チーク、アイブロウ(ランドマーク追従) |
| 全体 | フィルター(LUT)、明るさ・彩度・コントラスト、トリミング、回転 |

### フェーズ1への追加(承認済み)

| カテゴリ | 機能 |
|---|---|
| 構図 | 反転・回転(90°単位)・傾き補正(±30°)・縦横比プリセットによる中央切り抜き。ピンチで拡大・移動して仕上がりを確認できる |
| 背景・証明写真 | 背景のぼかし・単色化(Vision の人物切り抜き)、証明写真の自動切り出し(35×45mm / 30×40mm)。ポートレートモードの写真は、埋め込まれた切り抜き・深度を優先して使う |
| 文字 | 文字入れ(システムフォントのみ。フォントは同梱しない)。位置・大きさ・色・書体・影 |
| ルック | 調整値のまとまりを端末内に保存し、他の写真や複数の写真に適用する |
| 肌・顔立ち | 血色調整、鼻(小鼻を細く)、鼻筋のハイライト |
| 質感 | フィルム粒子、光漏れ。フィルターは 18 種(すべて数式から生成した自作) |
| 書き出し | 形式(JPEG / HEIC / PNG)と画質の選択。位置情報(GPS)の削除(既定でオン。設定で変更可)。OS 標準の共有シートを開く |
| 設定 | 書き出しの扱いとプライバシーの説明 |

いずれも端末内で完結し、第2章の禁止事項に触れない。

**共有について**: iOS の共有シート(`UIActivityViewController`)は、送り先の選択も送信も OS とユーザーが行い、Nuvo は通信しない。したがって第4章「対象外」の「SNS連携機能」(アプリ自身が SNS と連携する機能)には当たらない。特定の SNS の SDK・API は使わない。

### フェーズ2: リアルタイムカメラ

フェーズ1の処理をカメラプレビューにリアルタイム適用する。**60fps維持が必須要件**。フェーズ1完成まで着手しない。

### 対象外(現時点)

動画編集、スタンプ/AR、コラージュ、SNS連携機能。(文字入れはスタンプに含めない。上の「フェーズ1への追加」を参照)

---

## 5. アーキテクチャ

```
Sources/
├── App/
│   └── App.swift
├── Features/
│   ├── Editor/
│   │   ├── EditorView.swift          # メイン編集画面
│   │   ├── EditorViewModel.swift     # 調整パラメータの状態管理
│   │   └── Components/               # スライダー、ツールバー、比較ボタン
│   ├── Camera/                       # フェーズ2
│   └── Settings/
├── Core/
│   ├── FaceAnalysis/
│   │   ├── FaceLandmarkDetector.swift  # Vision ラッパー
│   │   └── FaceGeometry.swift          # ランドマーク→領域マスク変換
│   ├── Rendering/
│   │   ├── RenderPipeline.swift        # 処理チェーンの構築・実行
│   │   ├── AdjustmentParameters.swift  # 全調整値を持つ単一の構造体
│   │   └── Filters/
│   │       ├── SkinSmoothing.swift
│   │       ├── FaceReshape.swift       # メッシュ変形
│   │       ├── MakeupOverlay.swift
│   │       └── LUTFilter.swift
│   ├── Shaders/
│   │   └── *.metal                     # カスタムカーネル
│   └── PhotoLibrary/
│       └── PhotoExporter.swift
└── Resources/
    ├── LUTs/                           # .cube / PNG形式のLUT
    ├── Assets.xcassets
    └── Localizable.xcstrings           # 日本語・英語
```

**設計の核**

- `AdjustmentParameters` は全調整値を保持する単一の値型。UIはこれを書き換えるだけ、レンダラはこれを読むだけ。Undo/Redoはこの構造体のスナップショットで実現する
- **元画像は絶対に破壊しない。** 加工は常に元画像 + パラメータから再生成する
- `RenderPipeline` はパラメータを受け取り加工済み画像を返す。UIの知識を持たない

---

## 6. 画像処理の実装方針

**ここが製品の品質を決める。安易な実装をしないこと。**

### 肌スムージング

- **単純なガウスぼかしは禁止。** 輪郭や目鼻まで溶けて「のっぺり」した安っぽい仕上がりになり、既存アプリとの差が一目でつく
- **バイラテラルフィルタ**(エッジ保存平滑化)または**周波数分離**(低周波=肌の色ムラだけを平滑化し、高周波=毛穴・質感を残す)を実装する
- Core Imageにバイラテラルフィルタの標準実装はない。**Metalでカスタム `CIKernel` を書くこと**
- 肌色領域マスクを併用し、髪・目・眉・唇・背景には適用しない。ランドマークからの領域マスク + 肌色域(YCbCr空間での判定)の積を取る
- 強度100%でも自然に見える上限を設ける。やりすぎると即座にチープに見える

### 顔立ち変形(小顔・デカ目)

- ランドマークから制御点を作り、**メッシュ変形(warp)** をMetalシェーダで実装する
- 背景の直線(ドア枠、柱など)が歪むと破綻が目立つ。変形の影響範囲を顔周辺に減衰させること
- 左右非対称な顔でも破綻しないよう、ランドマーク基準で変形する(画像中心基準にしない)

### フィルター

- `CIColorCube` / `CIColorCubeWithColorSpace` でLUTを適用する
- LUTはPNGのグリッド画像として同梱し、起動時ではなく**使用時に遅延ロード**する
- 自作LUTを使う。既存アプリのLUTを流用しないこと

### パフォーマンス

- プレビューは縮小画像(長辺1536px程度)、書き出し時のみフル解像度で再処理する
- `CIContext` は使い回す。毎回生成すると致命的に遅い
- 処理はバックグラウンド、UI反映のみ `@MainActor`
- スライダー操作中は間引き処理(throttle)を入れ、指を離した時点で高品質再処理する
- フェーズ2ではCPU経由を挟まず、`CVPixelBuffer` → Metal テクスチャで直接処理する

---

## 7. プライバシー関連の必須対応

### Info.plist

```xml
<key>NSPhotoLibraryAddUsageDescription</key>
<string>加工した写真を保存するために使用します。写真が端末の外に送信されることはありません。</string>
<key>NSCameraUsageDescription</key>
<string>撮影のために使用します。映像はすべて端末内で処理され、送信されることはありません。</string>
```

`PhotosPicker` を使う限り読み取り用の `NSPhotoLibraryUsageDescription` は不要。**必要最小限の権限のみ要求すること。**

### PrivacyInfo.xcprivacy

```xml
<key>NSPrivacyCollectedDataTypes</key>
<array/>
<key>NSPrivacyTracking</key>
<false/>
```

### App Store Connect

- App Privacy は「データを収集しない (Data Not Collected)」
- 説明文に「全機能無料・透かしなし・通信なし」を明記する
- アプリ表示名: **Nuvo**(公開前にApp Store・USPTO・J-PlatPatでの最終確認を行うこと)

---

## 8. UI方針

- 加工前後の比較は**長押しで元画像表示**(業界標準の操作。独自化しない)
- スライダーは中央がゼロの双方向にし、現在値を数値で表示する
- 調整項目は多すぎると迷うので、よく使う5項目を第一階層、残りを折りたたみに置く
- **課金導線がないぶん画面が素直になる。** 無料アプリにありがちな「機能を探させるUI」にしないこと

---

## 9. コーディング規約

- Swift API Design Guidelines に従う
- `force unwrap` (`!`) は禁止。`guard let` / `if let` を使う
- 1ファイル300行を目安に分割する
- コメントは「なぜそうしたか」を書く。特に画像処理のパラメータ定数には根拠を残す
- `print()` によるデバッグ出力を本番ビルドに残さない

---

## 10. テスト

- `FaceLandmarkDetector` の座標変換、`FaceGeometry` の領域マスク生成はユニットテスト必須
- 座標系の変換(Vision は左下原点の正規化座標、Core Image / UIKit とは異なる)は最頻出バグ。**変換は専用ヘルパーに集約し、テストを厚くする**
- レンダリング結果は基準画像との比較テスト(ゴールデンテスト)を用意する
- 実機でのfps計測をフェーズ2の受け入れ基準に含める

---

## 11. 完了の定義

- [ ] ビルドが警告なしで通る
- [ ] 既存テストが通り、新規ロジックにテストが追加されている
- [ ] **機内モードで全機能が正常に動作する**
- [ ] 新たなネットワーク通信・外部依存・課金要素が追加されていない
- [ ] 肌補正を最大値にしても不自然に見えない
- [ ] 日本語・英語の両ロケールで文字が破綻しない
- [ ] コード中に機密情報・個人情報がハードコードされていない(将来のOSS公開を前提とする)

---

## 12. エージェントへの依頼方法

- 作業前に関連ファイルを読むこと。推測で書き始めない
- 画像処理アルゴリズムの選定は、実装前に方針とトレードオフを提示して承認を得る
- 第2章の絶対制約に触れる可能性がある場合は、必ず手を止めて確認する

---

## 13. UI 試作アプリ(`prototype/`)

`prototype/` は Expo(React Native)による **UI の試作専用アプリ**で、製品には含まれない。Mac を使わない開発で、見た目・配置・操作感を Fast Refresh で素早く詰めるために置いている。

- 目的は UI の検討のみ。決まった内容は `Sources/`(SwiftUI)へ写す。製品のコードは Swift のままで、この節は第3章の技術スタックを変更しない。
- 第2章「サードパーティ依存の追加」の対象外。依存は `prototype/` の中だけに閉じ、アプリ本体(`Package.swift`・`project.yml`)には入れない。
- 画像処理は実装しない(値が動く手応えの確認用の簡易表示のみ)。
- 第2章のその他の禁止事項(通信・解析・課金・アカウントなど)は、試作にも同様に適用する。
- 色・余白などの値は `Sources/App/Theme.swift` と `prototype/src/theme.ts` を同じ値に保つ。

